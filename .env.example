PORT=5000
NODE_ENV=production
DATABASE_URL="file:./dev.db"

# Optional: OpenAI API Key for AI-assisted evaluations.
# If omitted or left blank, the platform uses its built-in rule-based evaluation engine.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
